type_barchart_stacked <- function(data, report_params, slide_params) {
  
  # values <- compute_values_set(data, report_params, slide_params)
  
  'Het functietype barchart_stacked is nog niet geimplementeerd'
  
}
